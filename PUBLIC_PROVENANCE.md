# Public Source Provenance

Exported from a private development baseline with a fresh public Git history. The export excludes private Git history, real meeting data, voiceprints, databases, credentials, signing material, model binaries and APKs.
